# Same-origin: il sito è servito dallo stesso backend, quindi API = /api
REACT_APP_BACKEND_URL=
# CRA: non trattare i warning come errori in build
CI=false
ESLINT_NO_DEV_ERRORS=true

DISABLE_ESLINT_PLUGIN=true
